name: tab_navigation_bar
description: Bottom navigation bar, it has unique indicator mode. When change current index then indicator move.
version: 1.0.7
repository: https://github.com/Mohiuddin655-PRO/flutter_tab_navigation_bar.git

environment:
  sdk: '>=3.1.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
